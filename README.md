# FUTURE_ML_02

This project builds a text classification system for customer support tickets using Natural Language Processing (NLP) and Machine Learning. The ticket text is preprocessed and vectorized using TF-IDF, then classified into categories using a Naive Bayes classifier. Tickets are also assigned a priority level (High, Medium, or Low) based on keywords, with results visualized through category and priority distribution charts.
